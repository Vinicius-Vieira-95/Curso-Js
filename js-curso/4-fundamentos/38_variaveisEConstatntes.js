// VAR é um identificador de uma variavel
// as variaveis devem conter nomes de acordo com a situação
//do seu projeto.

var a = 3;

//recomendavel usar vairiaveis unsando indentificador LET
let b = 4;

console.log(a+b);

// as variaveis podem ser reutilizadas quantas vezes for necessária
a = 300
b = 400

console.log(a, b);

// variaveis constantes não é permitido ser reutilizada por ser uma constante.
const c = 5;
console.log(c);